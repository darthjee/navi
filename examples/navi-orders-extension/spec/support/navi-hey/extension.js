// Mirror of source/lib/common/server/RequestHandler.js — the base class the
// running Navi image exposes as `navi-hey/extension`. Kept trivially small so
// the example's specs can import the backend module without a container.
class RequestHandler {
  handle() {}
}

export { RequestHandler };
