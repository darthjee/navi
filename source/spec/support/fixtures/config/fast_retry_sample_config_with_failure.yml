failure:
  threshold: 10.0
workers:
  quantity: 5
  retry_cooldown: 10
  sleep: 10
clients:
  default:
    base_url: https://example.com
    timeout: 5000
resources:
  categories:
    - url: /categories.json
      status: 200
