# Refactor the three RouteRegister specs
Rewrite the three spec files so each keeps its path and its top-level `describe('RouteRegister', ...)` but replaces the header and all scenarios with the helper:

```js
describe('RouteRegister', () => {
  const ctx = RouteRegisterUtils.setup(['get', 'post']);

  describe('#registerPost', () => {
    RouteRegisterUtils.itBehavesLikeRouteRegistration(ctx, { /* post verb config */ });
  });
});
```

- `RouteRegister_spec.js` (GET, `register`): `setup(['get', 'patch'])` to preserve the current stub, or `['get']` if `patch` is confirmed unused there; verb config uses `and.throwError(...)` and the invoke helper awaits a synchronous callback.
- `RouteRegister_patch_spec.js` (`registerPatch`): `setup(['get', 'patch'])`, `and.rejectWith(...)`.
- `RouteRegister_post_spec.js` (`registerPost`): `setup(['get', 'post'])`, `and.rejectWith(...)`.
- Remove imports the file no longer uses (`Logger`, error classes, `LogRegistry`, `RouteRegister`, `LoggerUtils`), so `eslint` `no-unused-vars` stays clean.
- Do not change any production file (`source/lib/server/RouteRegister.js` etc.).

Before removing the old scenarios, list each existing `it` title per file and confirm every one is generated by the shared example (the GET file's remaining lines after the unexpected-error `describe` were not fully read while planning; check them for extra cases and keep any that are not part of the shared set as explicit `it`s).

## Files to Change
- `source/spec/lib/server/RouteRegister_spec.js` — shrink to setup + shared example call, keep any GET-specific extras
- `source/spec/lib/server/RouteRegister_patch_spec.js` — shrink to setup + shared example call
- `source/spec/lib/server/RouteRegister_post_spec.js` — shrink to setup + shared example call
