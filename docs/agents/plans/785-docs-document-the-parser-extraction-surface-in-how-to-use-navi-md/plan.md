# Plan: docs: document the parser: / extraction surface in how_to_use_navi.md

Issue: [785-docs-document-the-parser-extraction-surface-in-how-to-use-navi-md.md](../../issues/785-docs-document-the-parser-extraction-surface-in-how-to-use-navi-md.md)

## Overview

Documentation-only sub-issue (1 of 2) of #783. Port the already-correct `parser:` /
`emit:` reference material from `README.md` into the `docs/guides/navi/` tree: a new
`extraction-configuration.md` sibling of `emit-configuration.md`, new `parser.*` /
`extraction.size` / `emit.size` rows in `prerequisites.md`, a Table-of-Contents entry plus
a short "`parser:` is required" note in `how_to_use_navi.md`, and three fixes to
`emit-configuration.md` (false "extraction is automatic" opening, missing cross-link, and a
`parser:` block added to its worked Example so the config is valid). No changes under
`source/` or `frontend/`.

See [docs.md](docs.md) for the full plan.
