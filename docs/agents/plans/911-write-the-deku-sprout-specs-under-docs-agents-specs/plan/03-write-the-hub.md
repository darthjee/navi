# Write the `deku-sprout` hub
Write `docs/agents/specs/deku-sprout.md` in the shape of `docs/agents/specs/crawler.md`: a title line, one or two sentences on what the feature is (extracting the shared logging code of `source/` and `clients/node/` into a public npm package, `deku-sprout`, in `logger/`), a note that it is a spec of not-yet-implemented work that is deleted once implemented (tracked by #888, removed by #917), and a `Topic | Description` table linking the six files under `docs/agents/specs/deku-sprout/`: `overview`, `package`, `migration`, `release-flow`, `rollout`, `decisions`.

## Files to Change
- `docs/agents/specs/deku-sprout.md` — new hub
