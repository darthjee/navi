# Part 6 — Documentation

## Goal

Update the dev app documentation to reflect the unified handler and registry design.

## Steps

- Update `docs/agents/dev-app.md` to describe the base `RequestHandler` class and the unified registry.
- Remove references to the separate `RedirectRegister` if it is deleted.
- Document the handler inheritance hierarchy.

## Files to Change

- `docs/agents/dev-app.md` — update routing section to reflect unified design
