# Plan: docs: surface the demo crawl-emit example

Issue: [781-docs-surface-the-demo-crawl-emit-example.md](../../issues/781-docs-surface-the-demo-crawl-emit-example.md)

## Overview

Add short pointers from the user-facing data-extraction/emission docs to the public `navi-hey`
demo, which now runs a live crawl-and-emit example end to end (dependencies #780 and #779 have both
merged). One note in `README.md` and three matching pointers across `docs/guides/navi/`. Docs only —
no code, engine, dev-app, or demo-config changes.

See [docs.md](docs.md) for the full plan.
