# Plan: Add examples

Issue: [751-add-examples.md](../../issues/751-add-examples.md)

## Overview

Add a `samples.md` index plus a `samples/` recipe tree under each of the two user-facing
guides (`docs/guides/navi/` for the engine, `docs/guides/navi-client/` for the client),
ship a first batch of 13 self-contained recipe files, wire the indexes into each guide's
table of contents, and add `Related sample:` back-links from the existing engine feature
guides. Docs-only, single owner.

See [docs.md](docs.md) for the full plan.
